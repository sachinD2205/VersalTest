import * as yup from "yup";
import FormattedLabel from "../../../containers/reuseableComponents/FormattedLabel";

// schema - validation
let SiteVisitAppointmentViewSchema = yup.object().shape({
 
});

export default SiteVisitAppointmentViewSchema;
