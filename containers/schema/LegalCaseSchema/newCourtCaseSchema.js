import * as yup from "yup";

// schema - validation
let schema = yup.object().shape({
  //   caseMainType: yup.string().required("Case Main Type is Required !!!"),

//   subType: yup.string().required("Case Main Type is Required !!!"),
});

export default schema;
